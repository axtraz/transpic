
[package]
authors = ["Axtraz"]
edition = "2021"
name = "transpic-core"
version = "0.1.0"

[lib]
name = "transpic_core"
crate-type = ["cdylib", "rlib"]

[dependencies]
image = "0.25.10"
napi = "3.0.0"
napi-derive = "3.0.0"

[build-dependencies]
napi-build = "2"

[profile.release]
lto = true
strip = "symbols"
