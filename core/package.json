{
  "name": "transpic-core",
  "version": "0.2.3",
  "description": "Native Node.js bindings for fast image manipulation, powered by Rust",
  "keywords": [
    "blur",
    "grayscale",
    "image",
    "image-processing",
    "napi",
    "napi-rs",
    "native",
    "resize",
    "rust"
  ],
  "scripts": {
    "artifacts": "napi artifacts",
    "build": "napi build --platform --release --output-dir bin",
    "build:debug": "napi build --platform",
    "format": "run-p format:oxfmt format:rs format:toml",
    "format:oxfmt": "oxfmt .",
    "format:toml": "taplo format",
    "format:rs": "cargo fmt",
    "lint": "oxlint .",
    "prepublishOnly": "napi prepublish -t npm",
    "test": "ava",
    "preversion": "napi build --platform && git add .",
    "version": "napi version",
    "prepare": "cd .. && husky"
  },
  "devDependencies": {
    "@emnapi/core": "^1.5.0",
    "@emnapi/runtime": "^1.5.0",
    "@napi-rs/cli": "^3.2.0",
    "@taplo/cli": "^0.7.0",
    "@tybys/wasm-util": "^0.10.0",
    "ava": "^8.0.0",
    "chalk": "^5.6.2",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.0",
    "npm-run-all2": "^8.0.4",
    "oxfmt": "^0.54.0",
    "oxlint": "^1.69.0",
    "oxlint-tsgolint": "^0.23.0",
    "tinybench": "^6.0.0",
    "typescript": "^6.0.0"
  },
  "optionalDependencies": {
    "transpic-core-darwin-arm64": "0.3.0",
    "transpic-core-linux-x64-gnu": "0.3.0",
    "transpic-core-linux-x64-musl": "0.3.0",
    "transpic-core-win32-arm64-msvc": "0.3.0",
    "transpic-core-win32-x64-msvc": "0.3.0"
  },
  "lint-staged": {
    "*.@(js|ts|tsx)": [
      "oxlint --fix"
    ],
    "*.@(js|ts|tsx|yml|yaml|md|json)": [
      "oxfmt"
    ],
    "*.toml": [
      "taplo format"
    ]
  },
  "napi": {
    "binaryName": "transpic-core",
    "targets": [
      "x86_64-pc-windows-msvc",
      "x86_64-unknown-linux-gnu",
      "x86_64-unknown-linux-musl",
      "aarch64-apple-darwin",
      "aarch64-pc-windows-msvc"
    ]
  },
  "ava": {
    "timeout": "2m",
    "workerThreads": false
  },
  "engines": {
    "node": ">= 18.17.0 < 19 || >= 20.3.0 < 21 || >= 21.1.0"
  },
  "packageManager": "pnpm@11.6.0"
}
